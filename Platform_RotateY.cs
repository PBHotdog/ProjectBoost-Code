using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Platform_RotateY : MonoBehaviour
{
    [SerializeField] float rotateSpeedY = 100f;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        obbyRotate();
    }
    void obbyRotate()
    {
    }
}

